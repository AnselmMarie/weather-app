import UiForecast from './forecast.view';
import forecastSlice from './forecast.slice';

// Action creators are generated for each case reducer function
export const { activeForecast } = forecastSlice.actions;

export { UiForecast };
